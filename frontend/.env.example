# Frontend env

# Vite proxies /api/* to this URL during dev. Defaults to http://localhost:4000.
# Set this if your backend runs on a different host/port.
# VITE_API_PROXY_TARGET=http://localhost:4000
